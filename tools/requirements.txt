# Exact pins matching tools/python-wheels (the known-good set).
# Keep these in sync when updating the vendored wheels.
n64img==0.3.3
numpy==2.2.5
mapfile_parser==2.12.1
Pillow==11.3.0
splat64[mips]==0.40.0
ipl3checksum==1.3.1
mido==1.3.3
